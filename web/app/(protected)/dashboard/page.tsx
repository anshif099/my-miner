"use client";
import { AlertTriangle, CircleDollarSign, Cpu, Radio } from "lucide-react";
import { PageHeading } from "@/components/page-heading";
import { effectiveAvailability, useDevices } from "@/hooks/use-devices";
export default function DashboardPage() { const { devices, loading, error } = useDevices(); const online = devices.filter((d) => effectiveAvailability(d) === "ONLINE").length; const running = devices.filter((d) => d.operationalState === "RUNNING").length; const cards = [{ label: "Total devices", value: loading ? "—" : devices.length, icon: Cpu }, { label: "Online devices", value: loading ? "—" : online, icon: Radio }, { label: "Mining devices", value: loading ? "—" : running, icon: AlertTriangle }, { label: "Today's earnings", value: "₹0.00", note: "No earnings yet", icon: CircleDollarSign }]; return <><PageHeading title="Overview" description="Actual device state from your Firebase project."/>{error && <p role="alert" className="panel mb-5 p-4 text-red-500">{error}</p>}<section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(({ label, value, note, icon: Icon }) => <article className="panel p-5" key={label}><div className="mb-6 flex items-center justify-between"><span className="muted text-sm">{label}</span><Icon size={19}/></div><strong className="text-3xl">{value}</strong>{note && <div className="muted mt-1 text-sm">{note}</div>}</article>)}</section><section className="panel mt-6 p-6"><div className="flex flex-wrap items-center justify-between gap-4"><div><h2 className="m-0 text-xl">Mining control</h2><p className="muted mb-0">No mining process is connected in Phase 2.</p></div><div className="flex flex-wrap gap-2"><button className="button" disabled>START · COMING IN PHASE 3</button><button className="button button-secondary" disabled>STOP · COMING IN PHASE 3</button><button className="button button-danger" disabled>EMERGENCY STOP · COMING IN PHASE 3</button></div></div></section><section className="panel mt-6 p-6"><h2 className="m-0 text-xl">Value summary</h2><div className="mt-5 grid gap-4 sm:grid-cols-2"><div><div className="muted text-sm">Estimated INR</div><strong className="text-2xl">Not available</strong></div><div><div className="muted text-sm">Realized crypto</div><strong className="text-2xl">No data yet</strong></div></div></section></>; }

