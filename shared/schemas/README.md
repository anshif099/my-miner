# Canonical contracts

`contracts.schema.json` is the language-neutral JSON Schema contract intended for future Rust and Kotlin contract tests. The TypeScript runtime schemas and inferred types live in `shared/src/index.ts` and enforce the same enums and required ownership fields.

